<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
        {# LOGO #}
        <img src="{{ asset('images/logo1.png') }}" alt="Logo" width="50" height="50">
        Adopte Auto-Mate
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">Accueil
                    </a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_trajets_index')}}">Mes Trajets</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_trajets_publics')}}">Trajets publics</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_trajets_prive')}}">Trajets privés</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_groupes.group')}}">Groupes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_notification')}}">Notifications</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="#">A propos</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('profil') }}">Mon compte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Se Connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>